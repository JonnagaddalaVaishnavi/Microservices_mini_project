spring:
  application:
    name: greet-api
  boot:
    admin:
      client:
        url:
        - http://localhost:1111/
server:
  port: 1011

management:
  endpoints:
    web:
      exposure:
        include:
        - '*'